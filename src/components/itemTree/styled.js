import { styled } from '@mui/material/styles';

export const StyledContainer = styled('div')({
  margin: 30,
  backgroundColor: '#e2e2e2',
  padding: 20,
});

export const TreeShift = styled('div')({
  marginLeft: 25
});

export const TreeContainer = styled('div')({
  marginTop: 30
});
